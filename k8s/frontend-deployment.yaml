apiVersion: apps/v1
kind: Deployment
metadata:
  name: pokereval-frontend
  namespace: default
  labels:
    app: pokereval-frontend
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pokereval-frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: pokereval-frontend
        version: "1.0.0"
    spec:
      containers:
        - name: frontend
          image: europe-west1-docker.pkg.dev/temp-convertor-485913/pokereval/frontend:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
